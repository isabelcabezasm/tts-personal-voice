# Azure Speech Service configuration for Personal Voice TTS
# Copy this file to .env and fill in your actual values.

# Speech Service subscription key (from Azure portal > your Speech resource > Keys)
SPEECH_KEY=your_speech_subscription_key_here

# Speech Service region (Personal Voice is available in: eastus, westeurope, southeastasia, westus2, eastasia)
SPEECH_REGION=eastus

# Speaker Profile ID from your Personal Voice
# For TRIAL voices (created via Speech Studio): Use the GUID from the trial API.
#   To find it: Open Speech Studio > Personal Voice > F12 Dev Tools > Network tab >
#   Reload page > Look for requests to /customvoice/trial/zeroshots/ > copy the GUID.
# For FULL voices (created via REST API): Use the speakerProfileId GUID.
SPEAKER_PROFILE_ID=your_speaker_profile_id_here

# Language for synthesis (e.g., en-US, es-ES, de-DE, etc.)
# See: https://learn.microsoft.com/azure/ai-services/speech-service/language-support?tabs=tts#personal-voice
SPEECH_LANGUAGE=en-US

# Speech style: Cheerful, Excited, Enthusiastic, Friendly, Prompt (neutral)
# Cheerful/Excited/Enthusiastic also apply subtle prosody boosts (rate + pitch)
SPEECH_STYLE=Cheerful

# Output audio format: wav or mp3
OUTPUT_FORMAT=wav

# Output file name (without extension, extension is added based on OUTPUT_FORMAT)
OUTPUT_FILENAME=output
